/*
 * Test génération de code : variables globales
 */

 glob entier i = 10*2+appel f(23)*lire()-21*appel g(a,21+21)/lire()*3; /* i = 0 normalement */
 glob entier j, k;

 fonction principal(){
	k = 1;
	j = k;
	k = i;
 }